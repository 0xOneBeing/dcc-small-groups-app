import { NotYetAvailable } from "@/components/leader/NotYetAvailable";

export default function CoordinatorFollowUpsPage() {
  return (
    <NotYetAvailable
      eyebrow="Coordinator"
      title="Follow-ups"
      blurb="First-timer / new-convert contact tracking and the overdue roll-up need follow-up endpoints, which the DCC API does not expose yet."
    />
  );
}
