import { NotYetAvailable } from "@/components/leader/NotYetAvailable";

export default function CellFollowUpsPage() {
  return (
    <NotYetAvailable
      title="Follow-ups"
      blurb="First-timer and new-convert assignments with contact tracking need follow-up endpoints, which the DCC API does not expose yet."
    />
  );
}
