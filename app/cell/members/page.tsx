import { NotYetAvailable } from "@/components/leader/NotYetAvailable";

export default function CellMembersPage() {
  return (
    <NotYetAvailable
      title="Cell members"
      blurb="The members register and 8-Sunday attendance strip need cell-member and attendance endpoints, which the DCC API does not expose yet."
    />
  );
}
